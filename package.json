{
  "name": "loopedin",
  "private": true,
  "version": "0.1.0",
  "description": "LoopedIn — paid community research. Members answer questions for cash & rewards; organizations ask their community and hear back, anonymized.",
  "workspaces": [
    "web",
    "server"
  ],
  "scripts": {
    "setup": "npm install && npm --workspace server run db:reset",
    "dev": "concurrently -n api,web -c green,cyan \"npm --workspace server run dev\" \"npm --workspace web run dev\"",
    "dev:api": "npm --workspace server run dev",
    "dev:web": "npm --workspace web run dev",
    "build": "npm --workspace web run build",
    "seed": "npm --workspace server run seed",
    "db:reset": "npm --workspace server run db:reset"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
